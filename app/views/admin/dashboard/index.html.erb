<% stat_cards = [
     { label: t("admin.stats.revenue"),   value: number_to_currency(@revenue), icon: "la-dollar-sign", color: "green",  sub: t("admin.stats.orders_count", count: @orders_count) },
     { label: t("admin.stats.orders"),    value: @orders_count,                icon: "la-shopping-bag", color: "indigo", sub: t("admin.stats.pending", count: @pending_orders) },
     { label: t("admin.stats.products"),  value: @stats[:products],            icon: "la-box",          color: "indigo", sub: t("admin.stats.published", count: @stats[:published]) },
     { label: t("admin.stats.low_stock"), value: @stats[:low_stock],           icon: "la-exclamation-triangle", color: "yellow", sub: t("admin.stats.out_of_stock", count: @stats[:out_of_stock]) }
   ] %>

<div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-5 mb-6">
  <% color_map = { "indigo" => "bg-indigo-500", "green" => "bg-green-500", "yellow" => "bg-yellow-500", "red" => "bg-red-500" } %>
  <% stat_cards.each do |card| %>
    <div class="bg-white rounded-xl shadow-sm p-5 flex items-center gap-4">
      <div class="w-12 h-12 rounded-lg <%= color_map[card[:color]] %> text-white grid place-items-center shrink-0">
        <i class="las <%= card[:icon] %> text-2xl"></i>
      </div>
      <div class="min-w-0">
        <div class="text-2xl font-bold text-gray-900 truncate"><%= card[:value] %></div>
        <div class="text-sm font-medium text-gray-600"><%= card[:label] %></div>
        <div class="text-xs text-gray-400"><%= card[:sub] %></div>
      </div>
    </div>
  <% end %>
</div>

<div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
  <!-- Category breakdown -->
  <div class="lg:col-span-2 bg-white rounded-xl shadow-sm p-6">
    <div class="flex items-center justify-between mb-5">
      <h3 class="font-semibold text-gray-900"><%= t("admin.dashboard.by_category") %></h3>
      <%= link_to t("admin.dashboard.manage"), admin_categories_path, class: "text-sm text-indigo-600 hover:underline" %>
    </div>
    <% if @category_breakdown.any? %>
      <div class="space-y-3">
        <% @category_breakdown.each do |name, count| %>
          <div>
            <div class="flex justify-between text-sm mb-1">
              <span class="text-gray-700"><%= name %></span>
              <span class="font-medium text-gray-900"><%= count %></span>
            </div>
            <div class="h-2.5 bg-gray-100 rounded-full overflow-hidden">
              <div class="h-full bg-indigo-500 rounded-full" style="width: <%= (count.to_f / @max_category_count * 100).round %>%"></div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <p class="text-sm text-gray-400"><%= t("admin.dashboard.no_categories") %></p>
    <% end %>

    <hr class="my-6">
    <h3 class="font-semibold text-gray-900 mb-4"><%= t("admin.dashboard.recently_added") %></h3>
    <div class="divide-y divide-gray-100">
      <% @recent_products.each do |product| %>
        <div class="flex items-center gap-3 py-2.5">
          <img src="<%= product_image_url(product) %>" class="w-10 h-10 rounded object-cover bg-gray-100">
          <div class="flex-1 min-w-0">
            <%= link_to product.name, admin_product_path(product), class: "text-sm font-medium text-gray-900 hover:text-indigo-600 truncate block" %>
            <span class="text-xs text-gray-400"><%= product.category.name %></span>
          </div>
          <%= boolean_badge product.published, true_text: t("admin.dashboard.live"), false_text: t("admin.dashboard.draft") %>
          <span class="text-sm font-semibold text-gray-700"><%= number_to_currency product.price %></span>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Side column -->
  <div class="space-y-6">
    <div class="bg-white rounded-xl shadow-sm p-6">
      <div class="flex items-center justify-between mb-4">
        <h3 class="font-semibold text-gray-900 flex items-center gap-2">
          <i class="las la-shopping-bag text-indigo-500"></i> <%= t("admin.dashboard.recent_orders") %>
        </h3>
        <%= link_to t("admin.dashboard.all"), admin_orders_path, class: "text-sm text-indigo-600 hover:underline" %>
      </div>
      <% if @recent_orders.any? %>
        <div class="space-y-3">
          <% @recent_orders.each do |order| %>
            <div class="flex items-center justify-between text-sm">
              <div class="min-w-0">
                <%= link_to order.number, admin_order_path(order), class: "font-medium text-gray-900 hover:text-indigo-600" %>
                <div class="text-xs text-gray-400 truncate"><%= order.customer_name %></div>
              </div>
              <div class="text-right shrink-0">
                <div class="font-semibold text-gray-900"><%= number_to_currency order.total %></div>
                <%= status_badge order.status.capitalize, color: order.status_color %>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <p class="text-sm text-gray-400"><%= t("admin.dashboard.no_orders") %></p>
      <% end %>
    </div>

    <div class="bg-white rounded-xl shadow-sm p-6">
      <h3 class="font-semibold text-gray-900 mb-4 flex items-center gap-2">
        <i class="las la-exclamation-triangle text-yellow-500"></i> <%= t("admin.dashboard.low_stock_alerts") %>
      </h3>
      <% if @low_stock_products.any? %>
        <div class="space-y-3">
          <% @low_stock_products.each do |product| %>
            <div class="flex items-center justify-between">
              <%= link_to product.name, admin_product_path(product), class: "text-sm text-gray-700 hover:text-indigo-600 truncate mr-2" %>
              <%= status_badge t("admin.dashboard.units_left", count: product.stock), color: :yellow %>
            </div>
          <% end %>
        </div>
      <% else %>
        <p class="text-sm text-gray-400"><%= t("admin.dashboard.well_stocked") %></p>
      <% end %>
    </div>

    <div class="bg-white rounded-xl shadow-sm p-6">
      <h3 class="font-semibold text-gray-900 mb-4 flex items-center gap-2">
        <i class="las la-star text-indigo-500"></i> <%= t("admin.dashboard.reviews_to_moderate") %>
      </h3>
      <% if @pending_reviews.any? %>
        <div class="space-y-3">
          <% @pending_reviews.each do |review| %>
            <div class="text-sm">
              <div class="flex items-center gap-1 text-yellow-500">
                <% review.rating.times { %><i class="las la-star text-xs"></i><% } %>
              </div>
              <p class="text-gray-700 truncate"><%= review.title.presence || review.body %></p>
              <span class="text-xs text-gray-400"><%= t("admin.dashboard.on_product", product: review.product.name) %></span>
            </div>
          <% end %>
        </div>
        <%= link_to t("admin.dashboard.moderate_all"), admin_reviews_path, class: "inline-block mt-4 text-sm text-indigo-600 hover:underline" %>
      <% else %>
        <p class="text-sm text-gray-400"><%= t("admin.dashboard.no_reviews") %></p>
      <% end %>
    </div>

    <div class="bg-white rounded-xl shadow-sm p-6">
      <h3 class="font-semibold text-gray-900 mb-4"><%= t("admin.dashboard.most_viewed") %></h3>
      <div class="space-y-2">
        <% @top_viewed.each_with_index do |product, i| %>
          <div class="flex items-center gap-2 text-sm">
            <span class="text-gray-400 w-4"><%= i + 1 %></span>
            <%= link_to product.name, admin_product_path(product), class: "flex-1 text-gray-700 hover:text-indigo-600 truncate" %>
            <span class="text-xs text-gray-400"><%= t("admin.dashboard.views", count: product.views_count) %></span>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
