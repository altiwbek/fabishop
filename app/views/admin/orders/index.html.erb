<div class="flex items-center gap-2 mb-6 flex-wrap">
  <% active = params[:status].blank? %>
  <%= link_to t("admin.orders.all_count", count: @status_counts.values.sum), admin_orders_path,
        class: "px-3 py-1.5 rounded-lg text-sm font-medium #{active ? 'bg-indigo-600 text-white' : 'bg-white text-gray-600 border border-gray-200 hover:bg-gray-50'}" %>
  <% Order.statuses.keys.each do |status| %>
    <% is_active = params[:status] == status %>
    <%= link_to admin_orders_path(status: status),
          class: "px-3 py-1.5 rounded-lg text-sm font-medium #{is_active ? 'bg-indigo-600 text-white' : 'bg-white text-gray-600 border border-gray-200 hover:bg-gray-50'}" do %>
      <%= t("admin.orders.statuses.#{status}") %> <span class="text-xs opacity-70">(<%= @status_counts[status] || 0 %>)</span>
    <% end %>
  <% end %>
</div>

<div class="bg-white rounded-xl shadow-sm overflow-hidden">
  <div class="overflow-x-auto">
    <table class="w-full text-sm">
      <thead class="text-left text-gray-500 border-b border-gray-100">
        <tr>
          <th class="px-4 py-3"><%= t("admin.orders.cols.order") %></th>
          <th class="px-4 py-3"><%= t("admin.orders.cols.customer") %></th>
          <th class="px-4 py-3"><%= t("admin.orders.cols.date") %></th>
          <th class="px-4 py-3"><%= t("admin.orders.cols.items") %></th>
          <th class="px-4 py-3"><%= t("admin.orders.cols.total") %></th>
          <th class="px-4 py-3"><%= t("admin.common.status") %></th>
          <th class="px-4 py-3"></th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100">
        <% @orders.each do |order| %>
          <tr class="hover:bg-gray-50">
            <td class="px-4 py-3 font-medium text-gray-900"><%= link_to order.number, admin_order_path(order), class: "hover:text-indigo-600" %></td>
            <td class="px-4 py-3 text-gray-600">
              <%= order.customer_name %>
              <div class="text-xs text-gray-400"><%= order.email %></div>
            </td>
            <td class="px-4 py-3 text-gray-500"><%= order.created_at.strftime("%b %d, %Y") %></td>
            <td class="px-4 py-3"><%= order.item_count %></td>
            <td class="px-4 py-3 font-semibold text-gray-900"><%= number_to_currency order.total %></td>
            <td class="px-4 py-3"><%= status_badge t("admin.orders.statuses.#{order.status}"), color: order.status_color %></td>
            <td class="px-4 py-3 text-right">
              <%= link_to t("admin.actions.view"), admin_order_path(order), class: "text-indigo-600 hover:underline" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <% if @orders.empty? %><div class="p-10 text-center text-gray-400"><%= t("admin.orders.empty") %></div><% end %>
</div>
<div class="mt-4"><%== pagy_tailwind_nav(@pagy) if @pagy.pages > 1 %></div>
