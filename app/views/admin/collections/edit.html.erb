<div class="flex items-center justify-between mb-6">
  <%= link_to "← Back to collections", admin_collections_path, class: "text-sm text-gray-500 hover:text-indigo-600" %>
  <%= button_to "Delete", admin_collection_path(@collection), method: :delete, class: btn_danger,
        form: { data: { turbo_confirm: "Delete this collection?" } } %>
</div>
<%= render "form" %>
