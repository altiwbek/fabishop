<div class="flex items-center justify-between mb-6">
  <%= link_to t("admin.form.back"), admin_collections_path, class: "text-sm text-gray-500 hover:text-indigo-600" %>
  <%= button_to t("admin.actions.delete"), admin_collection_path(@collection), method: :delete, class: btn_danger,
        form: { data: { turbo_confirm: t("admin.form.delete_collection") } } %>
</div>
<%= render "form" %>
